apiVersion: v1
kind: Service
metadata:
  name: real-estate-service
spec:
  type: LoadBalancer
  selector:
    app: real-estate
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30008